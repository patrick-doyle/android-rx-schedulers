apply plugin: 'com.jfrog.bintray'
apply plugin: 'com.github.dcendents.android-maven'

group = 'com.twistedequations'
version = project.property('maven.version')

install {
    repositories.mavenInstaller {
        pom.project {
            name 'Android Rx Schedulers'
            description 'Android Optimised Rx Schedulers'
            url 'https://github.com/TwistedEquations/android-rxmvl'
            inceptionYear '2016'

            packaging 'aar'
            groupId 'com.twistedequations.rx'
            version = project.property('maven.version')

            licenses {
                license {
                    name 'The Apache Software License, Version 2.0'
                    url 'http://www.apache.org/licenses/LICENSE-2.0.txt'
                    distribution 'repo'
                }
            }

            scm {
                connection 'https://github.com/TwistedEquations/android-rxmvl.git'
                url 'https://github.com/TwistedEquations/android-rxmvl'
            }

            developers {
                developer {
                    name 'Patrick Doyle'
                }
            }

        }
    }
}

task sourcesJar(type: Jar) {
    from android.sourceSets.main.java.srcDirs
    classifier = 'sources'
}

task javadoc(type: Javadoc) {
    source = fileTree("${project.rootDir}/Libraries")
    include( "**/*.java" )
    exclude "**/build/**","**/tests/**", "**/org/apache/http/**", "**/org.apache.http/**"
    classpath += project.files(android.getBootClasspath())
    failOnError false
    options.links("http://docs.oracle.com/javase/7/docs/api/");
    options.links("http://d.android.com/reference/");
}

task javadocJar(type: Jar, dependsOn: javadoc) {
    classifier = 'javadoc'
    from javadoc.destinationDir
}

artifacts {
    archives javadocJar
    archives sourcesJar
}

bintray {
    user = project.property('bintray.user')
    key = project.property('bintray.apikey')

    configurations = ['archives']
    pkg {
        repo = 'maven'
        name = 'andorid-schedulers'
        userOrg = user
        licenses = ['Apache-2.0']
        vcsUrl = 'https://github.com/TwistedEquations/android-rxmvl.git'
        publish = true
    }
}